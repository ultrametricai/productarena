packages:
  - .
  - mcp
  - cli

allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true

# NOTE: the lint toolchain's `typescript` resolution is pinned to 6.0.x by .pnpmfile.cjs
# (typescript-eslint has no TS 7 support yet) — see the comment there before touching either.
pnpmfile: scripts/pnpmfile.cjs
