## Summary

<!-- What does this PR change, and why? -->

## Checklist

- [ ] `pnpm test` passes locally
- [ ] `pnpm build` passes locally
- [ ] If evidence changed for any product, affected cells were re-judged
      (`pnpm pipeline judge --category <id> --product <id>`) and rankings were
      re-derived (`pnpm pipeline derive --category <id>`) — see CONTRIBUTING.md
- [ ] No hand-edited `verdicts.json` or `rankings.json` — verdicts come from the judge
      pipeline, rankings are derived output; both are regenerated by tooling, not
      typed by hand
- [ ] If this touches `data/`, the rankings determinism check would pass
      (`pnpm tsx pipeline/scripts/recompute-check.ts`)
