$ curl -si -X POST https://mcp.chargebee.com/mcp -H 'Content-Type: application/json' -d '<jsonrpc initialize>'  # Chargebee's hosted MCP, OAuth-gated with a tool:invoke scope
HTTP/2 401

www-authenticate: Bearer resource_metadata="https://mcp.chargebee.com/.well-known/oauth-protected-resource", scope="tool:invoke"

access-control-expose-headers: mcp-session-id, WWW-Authenticate
