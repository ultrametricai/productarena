$ curl -si -X POST https://mcp.recurly.com/mcp -H 'Content-Type: application/json' -d '<jsonrpc initialize>'  # the Compass public MCP server Recurly documents
HTTP/2 401

www-authenticate: Bearer resource_metadata="https://mcp.recurly.com/.well-known/oauth-protected-resource"
