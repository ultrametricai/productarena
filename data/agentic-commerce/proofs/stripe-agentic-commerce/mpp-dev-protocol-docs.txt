$ curl -s https://mpp.dev/ | head -4  # the MPP spec site answers agents in markdown
# MPP — Machine Payments Protocol

MPP (Machine Payments Protocol) is the open standard for machine-to-machine payments via HTTP 402.
