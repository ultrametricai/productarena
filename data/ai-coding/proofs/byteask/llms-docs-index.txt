$ curl -s https://byteask.ai/llms.txt | head -4
# ByteAsk
> An AI coding agent for C and C++. It edits your repository from the terminal, then drives the real toolchain (the compiler, the sanitizers, gdb, and your test suite) before it shows you a diff.

## About
