$ curl -si -X POST https://mcp.honeybadger.io/mcp -H 'Content-Type: application/json' -d '<jsonrpc initialize>'  # Honeybadger's hosted MCP server, [redacted]less → OAuth challenge
HTTP/2 401

www-authenticate: Bearer resource_metadata="https://mcp.honeybadger.io/.well-known/oauth-protected-resource/mcp"
